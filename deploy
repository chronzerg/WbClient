git checkout --orphan gh-pages
git rm -r --cached .
git add index.html js/*.js css/*.css assets/*.svg
git commit -m "deploy"
git push origin +gh-pages:gh-pages
git checkout -f master
git branch -D gh-pages